%article.word
  .word-graph

    %h1
      %span.pre Words related to
      %span.word{:'data-word-id' => @word.id, :'data-word-url' => word_url(@word)}
        = @word

    %ul
      - @word.related_words.each do |w|
        %li{ class: 'word',
             :'data-word-id' => w.id,
             :'data-word-url' => word_url(w) }
          = link_to w, word_path(w)

    .word.add{ :'data-word-id' => 'add' }
      = form_tag related_words_path @word do
        = text_field_tag :word
        = submit_tag '+'

= link_to 'Edit', edit_word_path(@word)
= link_to 'Back', words_path
%p#notice= notice
